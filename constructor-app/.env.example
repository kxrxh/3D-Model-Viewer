# Optional: Port for the server (default: 3000)
PORT=3000

# Optional: Path to Whisper models directory (default: ./models)
WHISPER_MODELS_PATH=./models

# Optional: Default Whisper model to use (default: small)
# Options: tiny, base, small, medium, large
WHISPER_MODEL=small 